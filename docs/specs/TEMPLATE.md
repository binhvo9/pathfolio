# Feature: <name>

Status: Draft | In progress | Done
Service: <which service owns this — e.g. User & Onboarding, Portfolio Simulation>

## Problem / goal

What user need does this solve, in one or two sentences.

## Scope

What's in. What's explicitly out (push to Icebox in BACKLOG.md if relevant).

## Behavior

Walk through the feature step by step — inputs, what happens, outputs.
Cover the happy path first, then edge cases / error states.

## Data

New or changed fields, tables, events. Link to the ERD if it affects it.

## API / interface

Endpoints, event names/payloads, or UI states this feature introduces.

## Open questions

Anything not yet decided. Remove this section once resolved.
